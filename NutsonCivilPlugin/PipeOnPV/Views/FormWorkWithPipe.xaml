<Window x:Class="NutsonCivilPlugin.PipeOnPV.Views.FormWorkWithPipe"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:NutsonCivilPlugin.PipeOnPV.ViewModels"
        xmlns:controls="clr-namespace:NutsonCivilPlugin.PipeOnPV.Views.Controls"
        d:DataContext="{d:DesignInstance Type=vm:ViewModelPipeOnPV}"
        mc:Ignorable="d"
        x:Name="mainWindow"
        Background="AliceBlue"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen">
   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="35" />
         <RowDefinition Height="*" />
         <RowDefinition Height="auto" />
      </Grid.RowDefinitions>

      <Grid>
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
         </Grid.ColumnDefinitions>
         <TextBlock Margin="5"
                    HorizontalAlignment="Left"
                    TextWrapping="Wrap"
                    MinWidth="200"
                    Text="{Binding Path=ProfileView.Name}"
                    TextAlignment="Center" />

         <Button HorizontalAlignment="Right"
                 Grid.Column="1"
                 Margin="5"
                 Content="Выбрать вид профиля"
                 Command="{Binding SelectProfileViewCommand}" />
      </Grid>

      <TabControl Grid.Row="1"
                  TabIndex="{Binding TabIndex}"
                  Background="Transparent">
         <TabItem Header="Трубопроводы">
            <controls:PipeInfo DataContext="{Binding PipesVM}"/>
         </TabItem>

         <TabItem Header="Колодцы">
            <controls:StructureInfo DataContext="{Binding StructuresVM}"/>
         </TabItem>
      </TabControl>
      <Button Grid.Row="2"
              Content="Применить"
              Margin="10,3"
              HorizontalAlignment="Right"
              Command="{Binding ModifyElementsCommand}" />
   </Grid>
</Window>
